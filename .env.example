VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production

# Token Sanity avec droits d'écriture (Editor), utilisé côté serveur uniquement
# par les fonctions /api/views et /api/likes pour incrémenter les compteurs.
# À créer dans manage.sanity.io > API > Tokens. NE PAS préfixer par VITE_.
SANITY_API_WRITE_TOKEN=
